import UIKit

struct Styles {
  static func configure() {
    UINavigationBar.appearance().barTintColor = .white
    UINavigationBar.appearance().tintColor = .mainRed
    UITabBar.appearance().tintColor = .mainRed
    UISearchBar.appearance().tintColor = .mainRed
    UIButton.appearance().tintColor = .mainRed
  }
}
